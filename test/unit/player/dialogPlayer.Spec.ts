'use strict';

describe('Player dialog', () => {

    //var dlg: jat.player.dialogPlayerCtrl, scope:ng.IScope, $modal: uib.IModalService<string>;

    //beforeEach(module('jat.player.dialog'));
    //beforeEach(module('ui.bootstrap.mock'));

    //beforeEach(inject(($rootScope: ng.IScope, $controller: ng.IControllerService, _$modal_: uib.IModalService<string>) => {
    //    scope = $rootScope.$new();
    //    dlg = $controller('dialogPlayerCtrl', {
    //        '$scope': scope
    //    });
    //    $modal = _$modal_;
    //}));

    //////Mock $modal
    ////var modalOpenMock: uib.IModal<string> = {
    ////    close: function (result?: string): void {
    ////        this.result.successCallback(result);
    ////    },
    ////    dismiss: function (reason: string): void {
    ////        this.result.errorCallback(reason);
    ////    },
    ////    result: {
    ////        then: function (successCallback: (promiseValue: Object) => ng.IPromise<string>, errorCallback?: (reason: any) => string): ng.IPromise<string> {
    ////            this.successCallback = successCallback;
    ////            this.errorCallback = errorCallback;
    ////            return null;
    ////        }
    ////    },
    ////    opened: null
    ////};
    ////beforeEach(inject(function ($modal: uib.IModalService<string>) {
    ////    spyOn($modal, 'open').and.returnValue(modalOpenMock);
    ////}));

    //it('should edit the player', () => {

    //    //$modal.open()
    //    //dlg.player
    //    //dlg.categories

    //});
});