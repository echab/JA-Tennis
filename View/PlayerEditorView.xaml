<UserControl x:Class="JA_Tennis.View.PlayerEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    Width="400" Height="300">
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding ResourceLabelName}" TextAlignment="Right" VerticalAlignment="Stretch"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" MaxHeight="20" VerticalAlignment="Top" VerticalContentAlignment="Top" />
    </Grid>
</UserControl>
