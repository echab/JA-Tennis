declare namespace models {
 
    export const enum DrawType { Normal, Final, PouleSimple, PouleAR }

    export const enum GenerateType { None, Create, PlusEchelonne, PlusEnLigne, Mix }

    export const enum ModelType { None, Tournament, Player, Event, Draw, Match, Box }

    export const enum Mode { Build, Plan, Play, Lock }
}